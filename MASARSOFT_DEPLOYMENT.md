# خطوات الرفع على charity.masarsoft.io

## ⚠️ تحذير مهم

هذا المشروع مضبوط الآن للعمل على النطاق:
```
https://charity.masarsoft.io/
```

جميع الإعدادات تم ضبطها لهذا المسار المحدد.

---

## 📋 خطوات الرفع

### 1. تجهيز الملفات

تأكد من أن لديك:
```
✅ جميع ملفات المشروع من GitHub
✅ ملف .env محدّث ببيانات قاعدة البيانات
✅ ملف .htaccess في المجلد الرئيسي
✅ ملف index.php في المجلد الرئيسي
```

### 2. الاتصال بـ Hosting عبر SSH

```bash
ssh user@charity.masarsoft.io
cd public_html  # أو أي مسار تم تحديده
```

### 3. تحميل الملفات

استخدم SCP أو FTP:
```bash
# عبر SCP
scp -r New-Charity_app/* user@charity.masarsoft.io:~/public_html/

# أو استخدم FTP/SFTP من برنامج مثل FileZilla
```

### 4. تشغيل الأوامر الضرورية

```bash
# الدخول للمجلد
cd public_html

# تثبيت التبعيات
composer install --optimize-autoloader --no-dev

# إنشاء encryption key (إذا لم تكن موجودة)
php artisan key:generate

# تشغيل قاعدة البيانات
php artisan migrate

# إنشاء البيانات الافتراضية
php artisan db:seed

# تحسين الأداء
php artisan config:cache
php artisan route:cache
php artisan view:cache

# تصفية الـ cache (احتياطي)
php artisan cache:clear
```

### 5. ضبط الأذونات

```bash
# الأذونات الصحيحة للمجلدات
chmod -R 755 public/
chmod -R 755 storage/
chmod -R 755 bootstrap/cache/

# في حالة وجود مشاكل، جرّب:
chmod -R 777 storage/
chmod -R 777 bootstrap/cache/
```

### 6. إنشاء المجلدات المطلوبة

```bash
mkdir -p storage/app/public
mkdir -p storage/logs
mkdir -p bootstrap/cache

# إذا كانت موجودة بالفعل، لا بأس
```

### 7. إعدادات SSL/HTTPS

تأكد من:
- ✅ تفعيل HTTPS على النطاق
- ✅ وجود شهادة SSL صحيحة
- ✅ إعادة التوجيه من HTTP إلى HTTPS

### 8. اختبار الموقع

افتح المتصفح وادخل:
```
https://charity.masarsoft.io/
```

يجب أن ترى صفحة تسجيل الدخول

---

## 🔐 بيانات تسجيل الدخول الافتراضية

```
البريد الإلكتروني: donia.a5ra2019@gmail.com
كلمة المرور: 123456789
```

⚠️ **غيّر كلمة المرور فوراً بعد أول تسجيل دخول!**

---

## ✨ ملفات التكوين

| الملف | الدور |
|------|------|
| `.env` | متغيرات البيئة والإعدادات الحساسة |
| `.htaccess` | توجيه الطلبات إلى public/index.php |
| `index.php` | نقطة الدخول للتطبيق |
| `public/.htaccess` | معالجة طلبات الملفات الثابتة |
| `public/storage/.htaccess` | توجيه طلبات التخزين |

---

## 🛠️ استكشاف المشاكل

### مشكلة: 500 Internal Server Error

```bash
# افتح log الأخطاء
cat storage/logs/laravel.log | tail -50

# تصفية الـ cache
php artisan cache:clear
php artisan config:clear

# تحقق من الأذونات
chmod -R 777 storage/
```

### مشكلة: Composer error "exec() not available"

**لا تشغّل:**
```bash
❌ php artisan storage:link  # سيفشل!
```

**الحل:** تم دمج حل في `public/storage/serve.php` - لا تحتاج لفعل شيء

### مشكلة: الصور لا تظهر

```bash
# تحقق من المجلدات
ls -la storage/app/public/

# تأكد من الأذونات
chmod -R 755 storage/app/public/

# اختبر الدخول المباشر
curl https://charity.masarsoft.io/storage/logos/your-image.png
```

### مشكلة: Database connection error

```bash
# تحقق من بيانات .env
grep DB_ .env

# اختبر الاتصال
php artisan db:show
```

---

## 📝 ملفات الدعم الإضافية

في المستودع توجد ملفات تفصيلية:

- **HOSTING_SETUP.md** - إرشادات شاملة
- **PRE_DEPLOYMENT.md** - قائمة تحقق كاملة
- **HOSTING_NOTES.md** - شرح تفصيلي لمشكلة التخزين
- **DEPLOYMENT_SUMMARY.txt** - ملخص سريع

---

## 🚀 بعد الرفع الناجح

### 1. تأكد من:
- [ ] الموقع يفتح بدون أخطاء
- [ ] تسجيل الدخول يعمل
- [ ] رفع الصور يعمل بشكل صحيح
- [ ] الصور تظهر في الواجهة

### 2. خطوات الأمان:

```bash
# غيّر كلمة المرور الافتراضية
# - سجّل دخول بـ donia.a5ra2019@gmail.com
# - اذهب إلى profile settings
# - غيّر كلمة المرور

# تعطيل APP_DEBUG (يجب أن يكون false في .env)
APP_DEBUG=false

# تفعيل HTTPS (يجب أن يكون في .env)
APP_URL=https://charity.masarsoft.io
```

### 3. النسخ الاحتياطية:

```bash
# أنشئ نسخة احتياطية دورية من قاعدة البيانات
mysqldump -u username -p database_name > backup.sql

# احفظ النسخة الاحتياطية في مكان آمن
```

---

## 📞 في حالة المشاكل

1. تحقق من ملف `storage/logs/laravel.log`
2. اتبع الخطوات في **HOSTING_NOTES.md**
3. راجع **PRE_DEPLOYMENT.md** للقائمة الكاملة

---

## ✅ قائمة التحقق النهائية

- [ ] تم رفع جميع الملفات بنجاح
- [ ] تم تشغيل `composer install`
- [ ] تم تشغيل `php artisan migrate`
- [ ] تم تشغيل `php artisan db:seed`
- [ ] تم ضبط الأذونات بشكل صحيح
- [ ] الموقع يفتح بدون أخطاء
- [ ] تسجيل الدخول يعمل
- [ ] تم تغيير كلمة المرور الافتراضية
- [ ] HTTPS مفعّل ويعمل بشكل صحيح
- [ ] النسخ الاحتياطية جاهزة

---

**آخر تحديث:** 2026-02-01

**تم الضبط لـ:** https://charity.masarsoft.io/
